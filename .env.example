DATABASE_URL="postgresql://user:password@localhost:5432/futuro_exchange?schema=public"
PORT=3000
INVITE_SECRET=

# Settlement (worker only). If unset, settlement queue disabled.
REDIS_URL=redis://localhost:6379

# Agent Beta
FUTURO_ADMIN_KEY=
AGENT_STARTING_BALANCE=10000
# ORACLEBOOK_URL=https://app.oraclebook.xyz   # API base URL for agents (optional; defaults to localhost)
# AGENT_OPS_CONTACT=james@oraclebook.xyz      # Shown to agents in profile for support
